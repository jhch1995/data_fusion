ADD_SUBDIRECTORY(data_fusion)

AUX_SOURCE_DIRECTORY(. SRC_LIST)
ADD_LIBRARY(imu STATIC ${SRC_LIST})

IF (WIN32)
ELSEIF(ANDROID_ABI)
    TARGET_LINK_LIBRARIES(imu data_fusion)
ELSEIF(UNIX)    
    TARGET_LINK_LIBRARIES(imu data_fusion)
endif()	

INCLUDE_DIRECTORIES(${PROJECT_ROOT_PATH})
